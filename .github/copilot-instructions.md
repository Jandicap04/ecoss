# ECHO LOOP workspace

- Keep the project as a lightweight Vite vanilla JavaScript game.
- Preserve pointer and touch controls for the Canvas prototype.
- Keep game logic in `src/main.js` and presentation in `src/style.css`.
- Run `npm run build` after gameplay or UI changes.
